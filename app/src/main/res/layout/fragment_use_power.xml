<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/use_power_layout"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Fragments.UsePowerFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/abilities_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/players_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
    <RelativeLayout
        android:id="@+id/use_power_btn_box"
        android:elevation="3dp"
        android:layout_alignParentBottom="true"
        android:padding="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <ImageView
            android:id="@+id/use_power_submit_btn"
            android:backgroundTint="@color/colorPrimaryDark"
            android:layout_centerVertical="true"
            android:background="@drawable/ic_check"
            android:tint="@color/colorAccent"
            android:layout_alignParentRight="true"
            android:layout_width="30dp"
            android:layout_height="30dp"/>
        <ImageView
            android:id="@+id/use_power_back_btn"
            android:backgroundTint="@color/colorPrimaryDark"
            android:layout_centerVertical="true"
            android:background="@drawable/ic_chevron_left"
            android:layout_alignParentLeft="true"
            android:layout_width="30dp"
            android:layout_height="30dp"/>
        <TextView
            android:id="@+id/use_power_title"
            android:textColor="@color/colorAccent"
            android:textSize="15dp"
            android:layout_centerVertical="true"
            android:layout_centerHorizontal="true"
            android:gravity="center"
            android:text="sample title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </RelativeLayout>
</RelativeLayout>